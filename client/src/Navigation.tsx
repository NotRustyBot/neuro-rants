import { accentColor } from "./style";

export function Navigation() {
    return (
        <div style={{ background: "#000", color: accentColor, padding: 5, display: "flex" }}>
            <h1
                style={{
                    color: "#000",
                    background: accentColor,
                    padding: 2,
                    margin: 0,
                }}
            >
                Neuro-rants
            </h1>
            <div
            style={{
                display: "flex"
            }}
            >
                
            </div>
        </div>
    );
}
